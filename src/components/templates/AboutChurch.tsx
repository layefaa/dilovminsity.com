import React from 'react';
import {homeHeaderStyle} from "@/styles";

const AboutChurch = () => {
  return (
      <div>
        <h2 className={`${homeHeaderStyle}`}></h2>
      </div>
  );
};

export default AboutChurch;
